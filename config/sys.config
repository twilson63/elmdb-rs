[
  %% elmdb application configuration
  {elmdb, [
    %% Default environment options
    {default_env_opts, [{map_size, 1073741824}]}, % 1GB default
    
    %% Default database options
    {default_db_opts, [create]},
    
    %% Write buffer size for batching (number of operations)
    {write_buffer_size, 1000},
    
    %% Logging level (debug, info, warning, error)
    {log_level, error}
  ]}
].